generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:../config/app.db"
}

model bills {
  id                  Int         @id @default(autoincrement())
  bno                 String      @unique(map: "idx_bills_bno_unique")
  bdate               String
  supply              String
  addr                String?
  gstin               String?     @default("UNREGISTERED")
  state               String?
  pin                 Int?
  gtot                Float
  disc                Float?
  cgst                Float?
  usern               String
  sgst                Float?
  firm                String
  igst                Float?
  rof                 Float?
  ntot                Float
  btype               String      @default("SALES")
  order_no            String?
  order_date          String?
  dispatch_through    String?
  docket_no           String?
  vehicle_no          String?
  consignee_name      String?
  consignee_gstin     String?
  consignee_address   String?
  consignee_state     String?
  consignee_pin       String?
  reason_for_note     String?
  original_bill_no    String?
  original_bill_date  String?
  narration           String?
  status              String?     @default("ACTIVE")
  cancellation_reason String?
  cancelled_at        String?
  cancelled_by        Int?
  attachment_url      String?
  attachment_file_id  String?
  party_id            Int?
  oth_chg_json        String?
  gst_selection_json  String?
  reverse_charge      Int?        @default(0)
  created_at          String
  updated_at          String
  firm_id             Int?
  users               users?      @relation(fields: [cancelled_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  parties             parties?    @relation(fields: [party_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  firms               firms?      @relation(fields: [firm_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  stock_reg           stock_reg[]

  @@index([party_id], map: "idx_bills_party_id")
  @@index([bno], map: "idx_bills_bno")
  @@index([firm_id], map: "idx_bills_firm_id")
}

model firms {
  id           Int         @id @default(autoincrement())
  name         String      @unique(map: "sqlite_autoindex_firms_1")
  address      String?
  contact_info String?
  created_at   String
  updated_at   String
  bills        bills[]
  parties      parties[]
  stock_reg    stock_reg[]
  stocks       stocks[]
  users        users[]
}

model parties {
  id                Int          @id @default(autoincrement())
  supply            String
  addr              String?
  gstin             String?      @default("UNREGISTERED")
  state             String?
  state_code        Int?
  pin               Int?
  pan               String?
  contact           String?
  usern             String
  firm              String
  has_multiple_gsts Int?         @default(0)
  created_at        String
  updated_at        String
  firm_id           Int?
  bills             bills[]
  firms             firms?       @relation(fields: [firm_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  party_gsts        party_gsts[]

  @@index([gstin], map: "idx_parties_gstin")
  @@index([firm_id], map: "idx_parties_firm_id")
}

model party_gsts {
  id                Int     @id @default(autoincrement())
  party_id          Int
  gst_number        String
  state             String
  state_code        Int
  location_name     String
  address           String
  city              String
  pincode           String
  contact_person    String?
  contact_number    String?
  is_active         Int?    @default(1)
  is_default        Int?    @default(0)
  registration_type String? @default("regular")
  valid_from        String
  valid_to          String?
  last_used_date    String?
  transaction_count Int?    @default(0)
  parties           parties @relation(fields: [party_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

model request_logs {
  id         Int     @id @default(autoincrement())
  method     String
  url        String
  ip         String?
  username   String?
  user_agent String?
  timestamp  String  @default("datetime('now')")
}

model settings {
  id            Int     @id @default(autoincrement())
  setting_key   String  @unique(map: "sqlite_autoindex_settings_1")
  setting_value String
  description   String?
  created_at    String
  updated_at    String
}

model stock_reg {
  id             Int     @id @default(autoincrement())
  type           String
  bno            String
  bdate          String
  supply         String
  item           String
  item_narration String?
  pno            String?
  batch          String?
  oem            String?
  hsn            String
  qty            Float
  qtyh           Float
  uom            String
  rate           Float
  grate          Float?
  cgst           Float?
  sgst           Float?
  igst           Float?
  disc           Float?
  discamt        Float?
  total          Float
  mrp            Float?
  expiry_date    String?
  project        String?
  user           String
  firm           String
  stock_id       Int?
  bill_id        Int?
  created_at     String
  updated_at     String
  firm_id        Int?
  bills          bills?  @relation(fields: [bill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  stocks         stocks? @relation(fields: [stock_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  firms          firms?  @relation(fields: [firm_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([item], map: "idx_stockreg_item")
  @@index([bno], map: "idx_stockreg_bno")
  @@index([firm_id], map: "idx_stockreg_firm_id")
}

model stocks {
  id         Int         @id @default(autoincrement())
  item       String
  pno        String?
  oem        String?
  hsn        String
  qty        Float
  uom        String
  rate       Float
  grate      Float
  total      Float
  mrp        Float?
  batches    String?
  user       String
  created_at String
  updated_at String
  firm_id    Int?
  stock_reg  stock_reg[]
  firms      firms?      @relation(fields: [firm_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([item, firm_id], map: "idx_stocks_item_firm")
  @@index([firm_id], map: "idx_stocks_firm_id")
}

model users {
  id         Int     @id @default(autoincrement())
  fullname   String
  username   String  @unique(map: "sqlite_autoindex_users_1")
  email      String  @unique(map: "sqlite_autoindex_users_2")
  password   String
  firm_id    Int?
  role       Int?
  created_at String
  updated_at String
  bills      bills[]
  firms      firms?  @relation(fields: [firm_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}
